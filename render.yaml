services:
  - type: web
    name: sole-server
    env: node
    rootDir: backend/api
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
